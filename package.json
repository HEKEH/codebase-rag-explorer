{
  "name": "codebase-rag-explorer",
  "engines": {
    "bun": ">=1.3.11"
  },
  "packageManager": "bun@1.3.11",
  "private": true,
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if(!/bun\\//.test(ua)){console.error('This project must be installed with Bun. Please run: bun install'); process.exit(1)}\"",
    "dev": "bun run --parallel --filter './apps/*' dev",
    "dev:server": "bun run --filter @repo/server dev",
    "dev:web": "bun run --filter @repo/web dev",
    "build": "bun run --filter @repo/web build",
    "typecheck": "bun run --filter './packages/*' typecheck && bun run --filter './apps/*' typecheck",
    "test": "bun run --parallel --filter './packages/*' --filter './apps/*' test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
